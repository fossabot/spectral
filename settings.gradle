pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://repo.spectralpowered.org/' }
    }
}

rootProject.name = 'spectral'

include(":client")
include(":common")
include(":util")